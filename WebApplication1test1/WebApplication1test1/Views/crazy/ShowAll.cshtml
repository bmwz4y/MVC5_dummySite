@model IEnumerable<WebApplication1test1.Models.CrazyTable>

@{
    ViewBag.Title = "ShowAll";
}

<h2>ShowAll</h2>

<p>
    @Html.ActionLink("Create New", "Create")                 |
    @* the following to simulate lesson 29*@
    @Html.ActionLink("Group By Date", "CrazyByDate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Crazy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CrazyDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Crazy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CrazyDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
</table>
@*added at lesson 33*@
@Html.Label("Testing text", "Text to test using htmlhelpers overloaded with style css",new { style = "color:Green"})
@Html.Label("testing text 2", "another text to test class css styling", new { @class = "label label-default"})

@*copyright notice using last modified date for each view on it's own*@
@section copyWrite
    {
    @File.GetLastWriteTime(Server.MapPath(VirtualPath)).ToLongDateString()
}