@*this was added in lesson 72 *@

@{ViewBag.Title = "NotFound";

}<div class="jumbotron">
    <h3 class="text-center label-danger text-capitalize">The requested URL was not found, Please make sure of the address.</h3>
</div>

@*copyright notice using last modified date for each view on it's own*@
@section copyWrite
    {
    @File.GetLastWriteTime(Server.MapPath(VirtualPath)).ToLongDateString()
}